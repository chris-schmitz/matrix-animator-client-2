export class AnimationFrame {
    constructor(id, height, width, gridColors) {
        this.id = id
        this.height = height
        this.width = width
        this.gridColors = gridColors
    }
}